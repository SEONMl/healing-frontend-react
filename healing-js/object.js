var numArr = new Array();
var numArr = new Array(5);
var charArr = ['a','b','c'];
var numArr = Array(1,2,3,4,5);

numArr.concat(charArr); // 배열끼리 합치는 메서드

// Date 객체
var now = new Date(); 
now.toDateString();

// Math 객체
Math.random(); 

// window 객체
window.open("경로", "창 이름", "창 옵션");

// navigator 객체
// battery, cookieEnabled, geolocation ...

// history 객체 (읽기 전용)
// back() forward() go(-1)

// location 객체

// screen 객체